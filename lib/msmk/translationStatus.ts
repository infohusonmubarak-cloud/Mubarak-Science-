// All Rohingya (`rhg`) strings in content/msmk/**/*.ts are my best-effort
// draft translations, not the output of a certified Rohingya translator —
// I'm not one. Given this is life-safety content (first aid, CPR, infant
///elderly care), every `rhg` string must be checked by a native Rohingya
// speaker before launch, exactly like the medical content itself needs an
// MBBS doctor's sign-off. Both checks are structurally the same "review
// before trusting" gate; this constant is what TranslationNotice below
// reads to keep surfacing that until someone flips it.
//
// Flip this to true only once a native speaker has reviewed the `rhg`
// fields across content/msmk/**.
export const MSMK_TRANSLATIONS_REVIEWED = false;
