'use client';

import { StaffLoginForm } from '@/components/admin/StaffLoginForm';

export function AdminLogin() {
  return <StaffLoginForm heading="Staff Login" subheading="Sign in to review registrations and issue access codes." />;
}
