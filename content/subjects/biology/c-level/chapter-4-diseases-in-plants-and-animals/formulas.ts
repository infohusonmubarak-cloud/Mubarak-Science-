import type { Formula } from '@/types/content';

// This chapter is descriptive/comparative rather than calculation-based —
// no dedicated formula cards.
export const chapter4DiseasesFormulas: Record<string, Formula> = {};
