import type { Formula } from '@/types/content';

// This chapter is descriptive/mechanistic rather than calculation-based —
// no dedicated formula cards.
export const chapter5CoordinationFormulas: Record<string, Formula> = {};
