import type { Formula } from '@/types/content';

// This introductory chapter is conceptual rather than calculation-based —
// no dedicated formula cards.
export const chapter1ThemesFormulas: Record<string, Formula> = {};
