import type { Formula } from '@/types/content';

// This chapter is largely descriptive — transition-element chemistry at
// this level doesn't hinge on a dedicated formula the way stoichiometry or
// equilibrium does. The oxidation-state balancing method used in
// `variable-oxidation-states` is shown as a worked example instead.
export const chapter6TransitionFormulas: Record<string, Formula> = {};
