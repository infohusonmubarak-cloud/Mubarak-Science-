import type { Formula } from '@/types/content';

// Environmental chemistry at this level is descriptive rather than
// calculation-based — no dedicated formula cards for this chapter.
export const chapter7EnvironmentFormulas: Record<string, Formula> = {};
